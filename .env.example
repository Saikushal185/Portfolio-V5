# Copy to .env.local and fill in from the Supabase dashboard
# (Project Settings → API). The same project V3 uses.
#
# Without these the site still builds and runs — the contact box and /admin
# degrade gracefully via isSupabaseConfigured.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
